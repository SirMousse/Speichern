<?php
// Pokémon-Damage: Typ-Juggling, Vergleichsoperatoren, Rundung
// $attack = "55";     // absichtlich String
// $defense = 18;      
// $stab = 1.5;        // Same Type Attack Bonus
// $typeMultiplier = 2; // z.B. Elektro gegen Wasser

// Aufgaben
// 1. Berechne Schaden:  $raw = ($attack - $defense) * $stab * $typeMultiplier;

// 2. Gib var_dump($raw) aus (achte auf Datentypen).

// 3. Erzeuge:  - $damageFloor = (int) floor($raw);
//              - $damageRound = (int) round($raw);
//              - gib beide aus.

// 5. Vergleichs-Falle:
//      var_dump($attack == 55);
//      var_dump($attack === 55);

// Erkläre dir selbst anhand der Ausgabe, was passiert (du musst mir nichts schreiben, nur ausführen und checken).

?>